services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
    depends_on:
      - ssr

  ssr:
    build:
      context: ./tg-offliner-frontend
      dockerfile: Dockerfile.ssr
    ports:
      - "3000:3000"
    working_dir: /app
    command: npm run dev
    volumes:
      - ./tg-offliner-frontend:/app
      - /app/node_modules