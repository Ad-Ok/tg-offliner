# 🔧 Исправление экспорта HTML

## Проблема:
После рефакторинга пропали endpoints для экспорта HTML и PDF:
- `/api/channels/<channel_id>/export-html` - экспорт в HTML  
- `/api/channels/<channel_id>/print` - экспорт в PDF

Ошибка: `HTTP 404` при попытке экспорта HTML

## Решение:

### ✅ Восстановлены endpoints в `api/channels.py`:

#### 1. Экспорт HTML:
```python
@channels_bp.route('/channels/<channel_id>/export-html', methods=['GET'])
def export_channel_to_html(channel_id):
    # Получает HTML от SSR сервера
    # Обрабатывает для автономного использования
    # Копирует CSS файлы
    # Сохраняет в downloads/{channel_id}/index.html
```

#### 2. Экспорт PDF:
```python
@channels_bp.route('/channels/<channel_id>/print', methods=['GET'])
def print_channel_to_pdf(channel_id):
    # Получает HTML от SSR сервера с параметром pdf=1
    # Генерирует PDF с помощью weasyprint
    # Сохраняет в downloads/{channel_id}/{channel_id}.pdf
```

### ✅ Добавлена функция обработки HTML:
```python
def process_html_for_standalone(html_content):
    # Удаляет внешние CSS ссылки
    # Добавляет локальную ссылку на styles.css
    # Удаляет скрипты для статичного HTML
    # Обновляет пути к медиафайлам на относительные
```

### ✅ Очистка:
- Удален дублирующий файл `api/html.py` 
- Убрана регистрация html_bp из app.py
- Добавлен импорт `requests` в channels.py

## Результат:
Экспорт HTML и PDF снова работает! 📄✨

Теперь пользователи могут:
- Экспортировать каналы в автономный HTML формат
- Генерировать PDF файлы для печати
- Все файлы сохраняются в папку downloads/{channel_id}/
